import styled from 'styled-components';

const UserMeWrapper = styled.div`
  margin-top: 96px;
  padding: 14px var(--phone-margin);
  display: flex;

  .profile-link {
    padding-left: 12px;
    display: flex;
    flex-direction: column;
    justify-content: center;
    flex: 1;
  }
`;

export { UserMeWrapper };
